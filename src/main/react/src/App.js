import FromList from "./Component/Form/FromList";

function App() {
  /**
   * TODO:
   * 1. add /view, /edit, /viewresponse
   * 2. add options for select, long/short response
   */
  return (
    <div>
      <FromList />
    </div>
  );
}

export default App;
